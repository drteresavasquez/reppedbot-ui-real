// import React, { useState } from 'react';

export default function CalculateTasks() {
  // const [taskCount, setTaskCount] = useState(0);
  // make a call to API to get all tasks
  // count all tasks
  // filter all done tasks and get count

  // sort tasks based on username
  // then...count for all user tasks
  // then...count for all user done tasks
  return (
    <div>CalculateTasks</div>
  );
}
